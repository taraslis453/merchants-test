import { css } from "@emotion/react";

export const globalStyles = css`
   * {
     margin: 0;
     padding: 0;
   },
   body {
   background: linear-gradient(90deg, #fafafa 11px, transparent 1%) center, linear-gradient(#fafafa 11px, transparent 1%) center, #e5e5e5;
      background-size: 14px 14px;}
   
   background-attachment: fixed;
   min-height: 100vh;
       }
`;
